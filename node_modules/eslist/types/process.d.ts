import { Custom, State, Store } from './utils/manufacturer_collection';
export default function process<T, K>(store: Store<T, K>): {
    mapping: <L>(callbackfn: (data: T, state: State) => L) => L[];
    portal: (list: Array<T>, custom?: ((item: T) => Custom<T, K>) | undefined) => void;
    init: (list?: Array<T>, custom?: ((item: T) => Custom<T, K>) | undefined) => void;
    confirm: (key: string | number) => boolean;
    cancel: (key: string | number) => boolean;
};
