"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
function help(store) {
    function helper(id, content_helper) {
        var _a;
        var collection = store.get(id + '');
        if (content_helper && collection) {
            store.set(id + '', __assign(__assign({}, collection), { helper: typeof content_helper === 'object' ? __assign({}, content_helper) : content_helper }));
            return content_helper;
        }
        var h = (_a = store.get(id.toString())) === null || _a === void 0 ? void 0 : _a.helper;
        return h === undefined ? null : h;
    }
    function frozen(key) {
        var collection = store.get(key.toString());
        if (collection && collection.backup)
            return __assign(__assign({}, collection.backup), { key: key.toString() });
        return null;
    }
    return {
        helper: helper,
        frozen: frozen
    };
}
exports.default = help;
